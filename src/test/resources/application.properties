# Settings to connect to an H2 memory db with specific mode that emulates some things of a postgres db.
spring.datasource.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH

# Enable console to visualize data in h2 database
spring.h2.console.enabled=true